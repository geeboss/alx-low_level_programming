#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: betty wants to be understood further
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char gee;

	for (gee = 'a'; gee <= 'z'; gee++)
	{
		putchar(gee);
	}
	putchar('\n');
	return (0);
}
